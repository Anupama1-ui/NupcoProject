<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skyview.nupcoemployeeapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NupcoEmployeeApp" >
        <activity android:name=".Health" />
        <activity android:name=".ChatActivity"/>
        <activity android:name=".Notification"/>
        <activity android:name=".SettingActivity"/>
        <activity android:name=".MeetingActivity"/>
        <activity android:name=".TravelActivity"/>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplaschScreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>